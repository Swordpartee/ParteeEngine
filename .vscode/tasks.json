{
    "tasks": [
        {
            "type": "shell",
            "label": "Build with CMake",
            "command": "cmake",
            "args": ["--build", "build", "--config", "Debug"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$msCompile"],
            "detail": "Build with CMake"
        }
    ],
    "version": "2.0.0"
}