{
    "tasks": [
        {
            "type": "shell",
            "label": "Build with CMake",
            "command": "cmake",
            "args": ["--build", "build"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$gcc"],
            "detail": "Build with CMake"
        }
    ],
    "version": "2.0.0"
}